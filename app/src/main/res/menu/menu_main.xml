<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_show_old"
        android:title="@string/switch_show_old"
        android:checkable="true"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_near_only"
        android:title="@string/switch_near_only"
        android:checkable="true"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_month_only"
        android:title="@string/switch_month_only"
        android:checkable="true"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_demo_mode"
        android:title="@string/menu_demo_mode"
        android:checkable="true"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_about"
        android:title="@string/menu_about"
        app:showAsAction="never" />

</menu>
